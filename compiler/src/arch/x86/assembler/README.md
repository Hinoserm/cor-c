# x86 assembler

Intel-syntax 16/32-bit flat and relocatable assembly. CPU instruction profiles
are shared with the compiler backend; the default remains 486 plus x87.
The expression and operand parser currently remain in the legacy shared
assembler directory. See docs/X86-CPU-PROFILES.md for implementation status.
