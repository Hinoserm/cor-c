# x86 arch support

x86 ABI facts, backend implementation, encoding, and machine-level compiler
support. The default generated-code baseline is 486 plus x87; later
instructions require an explicit target policy. Focused x86 compiler tests are
in `../../../tests/arch/x86/`.
