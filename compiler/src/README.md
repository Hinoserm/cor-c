# Compiler sources

Target-independent compiler stages remain separate from architecture, platform,
ABI, and file-format code. Source files should normally match their top-level
type; purposeful partial-class subdivisions are retained.
