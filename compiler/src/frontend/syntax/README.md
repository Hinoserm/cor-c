# Syntax model

Standard C# syntax nodes and declaration records, one top-level type per file.
File-scope imports and partial declaration context remain attached to the nodes;
physical file splitting does not change language lookup semantics.
