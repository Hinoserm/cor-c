# Symbols and type semantics

Bound type/member identities, primitive types and numeric conversion rules.
One top-level type per file. These records currently remain resident during
whole-program binding; indexed demand loading is separate outstanding work.
