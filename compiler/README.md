# Compiler

The `corc` executable: frontend, lowering, IR, optimization, architecture
backends, object formats, assembler/disassembler, and linker. Production C#
sources live under `src/`; requirements are in `REQUIREMENTS.md`.
