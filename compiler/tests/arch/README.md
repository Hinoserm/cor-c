# Compiler arch tests

Focused tests for target-specific compiler support. Each target owns a
subdirectory here, keeping machine behavior close to the subsystem that
implements it without creating a repository-wide architecture grouping.
