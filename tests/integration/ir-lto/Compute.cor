public static class Compute
{
    public static int Twice(int value) => value * 2;
    public static int Choose(int value)
    {
        if (value > 0) return Twice(value);
        return -value;
    }
}
