# Native project pipeline

Standard .csproj fixtures compiled by COR-C#'s own evaluator and per-file driver.
Acceptance must cover native execution, unchanged builds and selective body-edit
invalidation without invoking MSBuild.
