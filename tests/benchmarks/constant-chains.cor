static class Program
{
    static long Work(int count, long seed)
    {
        long sum = 0;
        for (int i = 0; i < count; i++)
        {
            long x = (seed + i) & 65535;
            sum += (((x + 2147483647) + 13) - 2147483640) ^ ((x * 3) * 5);
        }
        return sum;
    }
    static long Reference(int count, long seed)
    {
        long sum = 0;
        for (int i = 0; i < count; i++)
        {
            long x = (seed + i) & 65535;
            sum += (x + 20) ^ (x * 15);
        }
        return sum;
    }
    static int Main()
    {
        string[] args = Environment.GetCommandLineArgs();
        int count = args.Length > 1 ? int.Parse(args[1]) : 1000000;
        long seed = args.Length > 2 ? long.Parse(args[2]) : 12345;
        if (count < 1 || count > 10000000 || seed < 0 || seed > 1000000) return 2;
        long start = Stopwatch.GetTimestamp();
        long result = Work(count, seed);
        long elapsed = Stopwatch.GetTimestamp() - start;
        bool valid = result == Reference(count, seed);
        Console.WriteLine("constant-chains,iterations=" + count + ",ticks=" + elapsed
            + ",frequency=" + Stopwatch.Frequency + ",checksum=" + result + ",valid=" + valid);
        return valid ? 0 : 1;
    }
}
