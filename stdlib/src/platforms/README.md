# Standard-library platforms

Internal adapters for platform-specific behavior. They implement ordinary
`System` contracts; this directory never defines substitute public APIs.

Internal adapters that translate host services and errors into standard .NET behavior.
