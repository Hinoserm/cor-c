# Linker command line

The standalone corlink executable consumes ELF relocatable objects without
loading the compiler. ObjectLinkCommand is shared with corc's convenience link
command; all link behavior is owned by the linker project.
