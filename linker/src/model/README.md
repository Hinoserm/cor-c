# Object model

Relocatable sections, symbols and relocations exchanged between code generation
and the linker. Existing public namespace names are preserved. These types have
no parser, binder, optimizer or managed IR dependency.
