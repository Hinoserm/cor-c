# Build runner tests

Focused behavioral checks for discovery, nested scope, ordering, cycles,
preflight validation, literal arguments, cleanup, timeout and test reports.
Run through `build test/unit/build-tool` or with the standard .NET project.
Temporary workspaces are created under the operating-system temporary directory.
