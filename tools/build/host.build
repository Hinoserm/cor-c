<Build FormatVersion="1" DefaultTargets="host">
  <Target Name="host" Description="Rebuild the hosted build utility using the owned project evaluator">
    <Compile Project="build.csproj" Toolchain="managed" />
  </Target>
</Build>
